# Calendar Memory
> Google Calendar is NOW LIVE — connected via Obsidian plugin + JARVIS Calendar API (token.json saved 2026-04-05).
> Last sync: not yet run — restart JARVIS and run sync_vault.py to populate.

## Today's Events
*(run sync_vault.py to populate — Calendar API credentials are ready)*

## Upcoming This Week
*(run sync_vault.py to populate)*

## Recently Booked
*(empty — updated when book_meeting tool is used)*

---
*Sources: JARVIS /calendar/today API + Obsidian Google Calendar plugin event notes*
*Written by sync_vault.py → sync_calendar()*
